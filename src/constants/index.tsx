export const HOME_FIELD_SENSORS = 'deviceId name serial region';
export const FULL_FIELD_SENSORS = 'deviceId name serial mac region longitude latitude floor distance remark optional active date live battery humidity temperature height maxHeight x y z tags deploymentDate compositionData unicastAddr meshStatus status error lockStatus network frequency server';

